{
  "_readme": [
    "Signed-off, EXPIRING justifications for advisories that npm audit reports against the",
    "production dependency closure but that cannot be reached in this system.",
    "",
    "Read this before adding an entry:",
    "",
    "1. A suppression is not 'this does not worry me'. It is a claim that a specific fact",
    "   about this codebase makes the advisory unreachable. That fact is the `premise`, and",
    "   scripts/security/checks/sca-suppression-premises.mjs re-asserts every one of them on",
    "   every commit. If the premise stops holding, the suppression is voided loudly and the",
    "   advisory is live again. A suppression with a premise nothing can assert is worthless,",
    "   which is why an unknown premise key is itself a finding.",
    "2. `expires` is mandatory and is enforced. Ninety days is the default. Nothing gets",
    "   buried permanently; someone re-reads the reasoning while it is still cheap to change",
    "   their mind about it.",
    "3. A suppression that matches no live advisory is also a finding. A stale waiver is a",
    "   loaded gun pointed at a future re-introduction of the same advisory.",
    "",
    "What is deliberately NOT suppressed, and why the gate is red on day one:",
    "the React Server Component denial-of-service family (GHSA-q4gf-8mx6-v5v3,",
    "GHSA-8h8q-6873-q5fj, GHSA-mg66-mrh9-m8jx) and the RSC cache-poisoning / cache-confusion",
    "family (GHSA-vfv6-92ff-j949, GHSA-wfc6-r584-vfw7, GHSA-68g3-v927-f742,",
    "GHSA-4633-3j49-mh5q). ib-api runs `next start` and serves RSC responses for real, so",
    "those are reachable. They are fixed by next 16.3.5. A gate that was green the day it",
    "shipped would have proved nothing."
  ],
  "defaultExpiryDays": 90,
  "premises": {
    "no-proxy-or-middleware": "There is no middleware.* or proxy.* file at the repo root, app/ or src/. Next 16 renamed middleware to proxy, so both names count. The Origin allowlist is implemented inside the route handlers, which is exactly the thing someone would be tempted to move into a proxy file.",
    "no-server-actions": "No 'use server' directive exists anywhere under app/, lib/ or components/. Every mutation on this site goes through an ordinary route handler under app/.",
    "images-unoptimized": "next.config.ts sets images: { unoptimized: true }, so the Image Optimization API is never mounted and sharp/libvips is never invoked. sharp is an optional dependency of next that nothing calls.",
    "no-rewrites-or-i18n": "next.config.ts declares no rewrites, no i18n and no cacheComponents.",
    "no-csp-nonce": "No nonce is generated or placed in any Content-Security-Policy, in next.config.ts, in the Apache .conf/.htaccess that sets the CSP for the static sites, or in app/layout.tsx. The policy is script-src 'self'.",
    "no-before-interactive": "No next/script uses strategy=\"beforeInteractive\".",
    "linux-host": "The only deploy target is the Ubuntu droplet, driven by systemd (scripts/deploy-api.sh uses systemctl; API-ON-DROPLET.md's unit is ExecStart=/usr/bin/node node_modules/.bin/next start). Nothing here runs on Windows.",
    "no-websocket-upgrades": "ib-api is plain `next start` behind an Apache reverse proxy that forwards /api/ only. There is no custom server file and no ws/socket.io/engine.io package in the production closure, so there is no WebSocket upgrade path to abuse.",
    "build-time-only": "The package executes only during `next build`, on a developer laptop. It is not a direct entry in package.json dependencies and is imported by nothing under app/, lib/ or components/. It is in the production closure only because npm resolves it as a transitive dependency of next; no deployed artifact loads it. (The static sites are plain files on Apache; ib-api serves prebuilt routes.)"
  },
  "suppressions": [
    {
      "ghsa": "GHSA-w5vr-8v7q-w6rv",
      "package": "baseline-browser-mapping",
      "premise": "build-time-only",
      "reason": "Process termination on invalid input. It is browserslist data consulted during the build; the only input is this repo's own browserslist config.",
      "expires": "2026-12-17"
    }
  ]
}
